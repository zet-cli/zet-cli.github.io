# Zet CLI

Astro-only public marketing site for [zet-cli](https://github.com/zet-cli).

## Development

```sh
npm ci --ignore-scripts
npm test
npm run build
```

The static site keeps public marketing separate from the user, organization, and Shared Auth surfaces.
